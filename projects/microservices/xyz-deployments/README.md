# xyz-deployments

Deployment companion repo for `xyz-service`.

This folder exists to validate the agentic workspace rule that service changes
must check deployment impact before completion.

Current sample assets:

```text
docker-compose.yml
k8s/xyz-service.yaml
```
