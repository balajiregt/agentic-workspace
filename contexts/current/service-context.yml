service_name: "xyz-service"

work_item:
  source: "jira"
  key: "ABC-123"
  summary: "add one more api test in CustomerRiskAPitest.java for riskCategory when it is medium"
  description: "Add or validate API coverage for the medium customer risk category."
  change_type: "test"
  affected_endpoints:
    - "GET /xyz"
  affected_fields:
    - "riskCategory"
  in_scope:
    - tests
  out_of_scope:
    - service_code
    - openapi_contract
    - deployment_change

repository_topology:
  service_and_tests: "split-qa"
  deployment: "separate-repo"

quality_gates:
  require_tests_for_behavior_change: true
  require_openapi_alignment: false
  require_deployment_impact_check: false
  require_hallucination_check: true
